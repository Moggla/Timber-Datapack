#> Slow chop on. The loop that does the work is started by the setting flip or by init in a real world, so start it here.
scoreboard players set slow_chop timber 1
scoreboard players set blocks_per_chop timber 20
function timber:slow_chop/loop
