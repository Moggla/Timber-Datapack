#> Back to the default
#> A found axe survives timber:axes/init like a custom one: uninstall clears the registry, init rebuilds it the way a load does
function timber:axes/uninstall
function timber:axes/init
