#> Back to the default
#> An axe the test added directly (not through the function tag) now survives timber:axes/init, so a plain reload isn't enough
#> any more to forget it. Uninstall clears the registry completely, then init rebuilds it the way a load does.
function timber:axes/uninstall
function timber:axes/init
