#> The group environments only split the tests into batches. Dozens of chops in the same tick were too much for the datapack,
#> a big tree now and then kept a few leaves. A batch has a fifth of the tests, nothing to set up.
