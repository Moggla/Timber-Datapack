#> The axe of the tests: a stick that mines logs (it gets the tool component when it is given), registered like the axe of a mod.
# Its item id has the namespace minecraft, so the scoreboard that says if it is enabled is called "stick".
function timber:api/add_axe {item:"minecraft:stick",durability:30,label:"Test Stick"}
